@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carousel Example</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick-theme.css">
    <style>
        /* General Container */
        .carousel-container {
            margin-top: 10px;
            padding: 0;
            width: 100%;
            position: relative;
        }

        /* Slick Slider */
        .slider {
            margin: 0;
            padding: 0;
            width: 100%;
        }

            .slider li {
                position: relative;
                display: inline-block;
                width: 100%;
            }

        /* Footer Styling (Name under the Image) */
        .footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            background-color: rgba(0, 0, 0, 0.7); /* Darker background for better visibility */
            color: white;
            text-align: center;
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 0 0 8px 8px;
            transition: opacity 0.3s ease-in-out; /* Smooth transition for visibility */
            opacity: 0; /* Footer initially hidden */
        }

        /* Make footer visible on hover */
        .slider li:hover .footer {
            opacity: 1; /* Footer becomes visible when hovered */
        }

        /* Slick Slider Adjustments */
        .slick-slide {
            transform: scale(0.8);
            opacity: 0.5;
            transition: transform 0.5s, opacity 0.5s;
            margin: 0;
        }

            .slick-slide.slick-center {
                transform: scale(1);
                opacity: 1;
            }

        /* Navigation Arrows */
        .slick-prev,
        .slick-next {
            position: absolute;
            top: 50%;
            cursor: pointer;
            outline: none;
            border-top: 2px solid #666;
            border-right: 1px solid #666;
            height: 15px;
            width: 15px;
            z-index: 10;
        }

        .slick-prev {
            left: 20px;
            transform: rotate(-135deg);
        }

        .slick-next {
            right: 40px;
            transform: rotate(45deg);
        }

    </style>
</head>
<body>
    <div class="carousel-container">
        <div class="col-lg-12 col-md-12 col-sm-12 mb-4">
            <div>
                <ul class="carousel slider">
                    <li>
                        <a href="#">
                            <img src="~/images/Alfredo.png" alt="Pasta">
                            <div class="footer">Pasta</div>
                        </a>
                    </li>
                    <li>
                        <a href="#">
                            <img src="~/images/Burger.png" alt="Burger">
                            <div class="footer">Burger</div>
                        </a>
                    </li>
                    <li>
                        <a href="#">
                            <img src="~/images/Pizza.jpg" alt="Pizza">
                            <div class="footer">Pizza</div>
                        </a>
                    </li>
                    <li>
                        <a href="#">
                            <img src="~/images/sandwich1.jpg" alt="Sandwich">
                            <div class="footer">Sandwich</div>
                        </a>
                    </li>
                    <li>
                        <a href="#">
                            <img src="~/images/kabab.jpg" alt="Kabab">
                            <div class="footer">Kabab</div>
                        </a>
                    </li>
                    <li>
                        <a href="#">
                            <img src="~/images/Lasagna.png" alt="Lasagna">
                            <div class="footer">Lasagna</div>
                        </a>
                    </li>
                    <li>
                        <a href="#">
                            <img src="~/images/Biryani2.webp" alt="Biryani">
                            <div class="footer">Biryani</div>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Include necessary JS -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
    <script>
        $(document).ready(function () {
            $(".slider").slick({
                autoplay: true,
                infinite: true,
                speed: 1000,
                slidesToShow: 3,
                slidesToScroll: 1,
                prevArrow: '<button class="slick-prev"></button>',
                nextArrow: '<button class="slick-next"></button>',
                centerMode: true,
                responsive: [
                    {
                        breakpoint: 1024,
                        settings: {
                            slidesToShow: 2,
                        },
                    },
                    {
                        breakpoint: 768,
                        settings: {
                            slidesToShow: 1,
                        },
                    },
                ],
            });
        });
    </script>
</body>
</html>
 *@
 <!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Menu</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f1e4;
            color: #333;
        }

        header {
            background-color: #8b4513;
            color: #fff;
            padding: 20px;
            text-align: center;
            font-size: 2em;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .carousel-container {
            max-width: 990px;
            margin: 20px auto;
            overflow: hidden;
            position: relative;        }

        .carousel-track {
            display: flex;
            transition: transform 0.5s ease-in-out;
            gap: 10px;
        }

        .carousel-slide {
            flex: 0 0 calc(25% - 10px); /* Show 4 slides at a time */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
            background-color: #fff;
            width: 30%;
            height: auto;
        }

        .carousel-slide img {
            width: 100%;
            height: auto;
        }

        .carousel-buttons {
            position: absolute;
            top: 50%;
            width: 100%;
            display: flex;
            justify-content: space-between;
            transform: translateY(-50%);
            pointer-events: none;
        }

        .carousel-button {
            background-color: rgba(0, 0, 0, 0.5);
            color: #fff;
            border: none;
            cursor: pointer;
            padding: 10px;
            font-size: 1.5em;
            border-radius: 50%;
            pointer-events: auto;
            transition: background-color 0.3s;
        }

        .carousel-button:hover {
            background-color: rgba(0, 0, 0, 0.7);
        }

        media (max-width: 768px) {
            .carousel-slide {
                flex: 0 0 calc(50% - 10px); /* Show 2 slides at a time for smaller screens */
            }
        }

        media (max-width: 480px) {
            .carousel-slide {
                flex: 0 0 calc(100% - 10px); /* Show 1 slide at a time for very small screens */
            }
        }
    </style>
</head>
<body>
    <header>Our Delicious Menu</header>
    <div class="carousel-container">
        <div class="carousel-track">
            <div class="carousel-slide"><img src="/images/breafast.jpg" alt="Image 1"></div>
            <div class="carousel-slide"><img src="/images/breafast.jpg" alt="Image 2"></div>
            <div class="carousel-slide"><img src="/images/festivals.jpg" alt="Image 3"></div>
            <div class="carousel-slide"><img src="/images/festivals.jpg" alt="Image 4"></div>
            <div class="carousel-slide"><img src="/images/brunch.jpg" alt="Image 5"></div>
            <div class="carousel-slide"><img src="/images/breafast.jpg" alt="Image 1"></div>
            <div class="carousel-slide"><img src="/images/breafast.jpg" alt="Image 2"></div>
            <div class="carousel-slide"><img src="/images/festivals.jpg" alt="Image 3"></div>
            <div class="carousel-slide"><img src="/images/festivals.jpg" alt="Image 4"></div>
            <div class="carousel-slide"><img src="/images/brunch.jpg" alt="Image 5"></div>
        </div>
        <div class="carousel-buttons">
            <button class="carousel-button" id="prev">&#10094;</button>
            <button class="carousel-button" id="next">&#10095;</button>
        </div>
    </div>

    <script>
        const track = document.querySelector('.carousel-track');
        const slides = Array.from(track.children);
        const nextButton = document.getElementById('next');
        const prevButton = document.getElementById('prev');

        const slideWidth = slides[0].getBoundingClientRect().width + 10; // Includes gap
        const visibleSlides = 4; // Number of slides visible at a time
        let currentIndex = 0;

        const updateButtons = () => {
            prevButton.disabled = currentIndex === 0;
            nextButton.disabled = currentIndex >= slides.length - visibleSlides;
        };

        const moveSlides = () => {
            track.style.transform = `translateX(-${currentIndex * slideWidth}px)`;
        };

        nextButton.addEventListener('click', () => {
            if (currentIndex < slides.length - visibleSlides) {
                currentIndex++;
                moveSlides();
                updateButtons();
            }
        });

        prevButton.addEventListener('click', () => {
            if (currentIndex > 0) {
                currentIndex--;
                moveSlides();
                updateButtons();
            }
        });

        // Initialize carousel
        updateButtons();
    </script>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carousel Example</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick-theme.css">

    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f1e4;
            color: #333;
        }

        

        .carousel-container {
            max-width: 1000px;
            margin: 20px auto;
            overflow: hidden;
            position: relative;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .carousel-track {
            display: flex;
            transition: transform 0.5s ease-in-out;
        }

        .carousel-slide {
            flex: 0 0 25%;
            text-align: center;
            position: relative;
            cursor: pointer;
        }

        .carousel-slide img {
            width: 100%;
            height: auto;
            border-radius: 10px;
        }

        .carousel-slide h2 {
            position: absolute;
            bottom: 10px;
            left: 50%;
            transform: translateX(-50%);
            background-color: rgba(0, 0, 0, 0.5);
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
        }

        .carousel-buttons {
            position: absolute;
            top: 50%;
            width: 100%;
            display: flex;
            justify-content: space-between;
            transform: translateY(-50%);
        }

        .carousel-button {
            background-color: rgba(0, 0, 0, 0.5);
            color: #fff;
            border: none;
            cursor: pointer;
            padding: 10px;
            font-size: 1.5em;
            border-radius: 50%;
            transition: background-color 0.3s;
        }
        .carousel-slide img {
    width: 100%;
    height: auto;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.carousel-slide:hover img {
    transform: scale(1.05);
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
}
.carousel-button {
    background: linear-gradient(135deg, #444, #222);
    color: #fff;
    border: none;
    cursor: pointer;
    padding: 12px;
    font-size: 1.5em;
    border-radius: 50%;
    pointer-events: auto;
    transition: background 0.3s, transform 0.2s;
}

.carousel-button:hover {
    background: linear-gradient(135deg, #666, #333);
    transform: scale(1.1);
}


.carousel-slide {
    margin :0;
    position: relative;
    transition: border 0.3s;
}

.carousel-slide img {
    border: 3px solid transparent;
}

.carousel-slide img.active {
    border: 3px solid #8b4513;
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
}

        
       /*  media (max-width: 768px) {
            .items {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }

            .carousel-slide {
                flex: 0 0 50%;
            }
        } */
    </style>
</head>
<body>
   
    <div class="carousel-container">
       
        <div class="carousel-track-container">
            <div class="carousel-track">
                <div class="carousel-slide" data-section-id="burgers" >
                    <img src="/images/Burger.png" alt="Burgers">
                    <h3>Burgers</h3>
                </div>
                <div class="carousel-slide" data-section-id="pizzas">
                    <img src="/images/mughlaiComp-pizza2.jpg" alt="Pizzas">
                    <h3>Pizzas</h3>
                </div>
                <div class="carousel-slide" data-section-id="rice">
                    <img src="/images/BoiledRice.jpg" alt="rice">
                    <h3>Rice</h3>
                </div>
                <div class="carousel-slide" data-section-id="handi">
                    <img src="/images/whiteChcken.jpg" alt="handi">
                    <h3>Chicken Handi</h3>
                </div>
                <div class="carousel-slide" data-section-id="appetizers">
                    <img src="/images/wings.jpg" alt="appetizers">
                    <h3>Appetizers</h3>
                </div>
                <div class="carousel-slide" data-section-id="desserts">
                    <img src="/images/lava cAke.jpg" alt="desserts">
                    <h3>Desserts</h3>
                </div>
                <div class="carousel-slide" data-section-id="dips">
                    <img src="/images/BBQSauce.jpg" alt="dips">
                    <h3>Dips</h3>
                </div>
                <div class="carousel-slide" data-section-id="Drinks">
                    <img src="/images/freshLime.jpg" alt="Drinks">
                    <h3>Drinks</h3>
                </div>
                <!-- Add more slides as needed -->
            </div>

        </div>
        <div class="carousel-buttons">
            <button class="carousel-button" id="prev">&#10094;</button>
            <button class="carousel-button" id="next">&#10095;</button>
        </div>
        
    </div>

   

   

    
   
</body>
</html>    